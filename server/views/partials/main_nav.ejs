<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <img class="navbar-brand" src="/Assets/images/logo.jpeg" id="logo">
      <a class="navbar-brand" href="#">COMP229</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if (title =="Home"){%>
            <li class="nav-item">
              <%} else {%>
              <a class="nav-link active" aria-current="page" href="/"><i class="fas fa-laptop house"></i>Home</a>
            <%}%>
            </li>
  
            <% if (title =="Surveys"){%>
              <li class="nav-item">
                <%} else {%>
                <a class="nav-link active" aria-current="page" href="/survey"><i class="fas fa-survey"></i>Surveys</a>
                <%}%>
              </li>

              <% if (title =="Answers"){%>
                <li class="nav-item">
                  <%} else {%>
                  <a class="nav-link active" aria-current="page" href="/answer"><i class="fas fa-answer"></i>Answers</a>
                  <%}%>
                </li>

            <% if(typeof displayName == 'string' && displayName) { %>
              <li class="navbar-text"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %></li>
              <li class="nav-item"><a href="/logout" class="nav-link">
                <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
            <% } else { %>
              <li class="nav-item"><a href="/login" class="nav-link">
                <i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
            <% } %>
      </div>
    </div>
  </nav>